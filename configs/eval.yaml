# @package _global_

defaults:
  - _self_
  - data: coco.yaml # choose datamodule with `test_dataloader()` for evaluation
  - model: sam_ft.yaml
  - callbacks: sam_ft.yaml
  - logger: wandb
  - trainer: gpu.yaml
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
